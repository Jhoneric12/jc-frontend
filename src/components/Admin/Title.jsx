import React from 'react'

function Title({title}) {
  return (
    <section>
        <h1 className='text-medium font-bold mb-6 font-font-color'>{title}</h1>
    </section>
  )
}

export default Title